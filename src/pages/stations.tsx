import SimpleLayout from "../layout/simple-layout";

// Create Gig Button if QC
// View Orders if multiple orders
// View Gigs if only one order
// Assign gig if leader, supervisor, or manager

const stationsPage = () => {
  return (
    <SimpleLayout>
      <h1>Stations Page</h1>
    </SimpleLayout>
  );
}

export default stationsPage;